import {
  clientSchema,
  clientWithoutPasswordSerializer,
  clientUpdateSchema,
} from "./client.schema";
import { contactsSchema, contactsUpdateSchema } from "./contacts.schema";

export {
  clientSchema,
  contactsSchema,
  clientWithoutPasswordSerializer,
  clientUpdateSchema,
  contactsUpdateSchema,
};
